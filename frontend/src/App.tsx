// Placeholder : les vrais ecrans (connexion, liste de conversations, fil de
// messages) arrivent aux taches suivantes du plan. Ce composant ne sert qu'a
// prouver que la chaine Vite + React + Tailwind est branchee de bout en bout.
export default function App() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-slate-900 text-slate-100">
      <h1 className="text-2xl font-semibold">Instant Messaging</h1>
    </main>
  );
}
